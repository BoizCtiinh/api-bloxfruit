services:
  - type: web
    name: bloxfruits-monitor
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: RENDER_EXTERNAL_URL
        sync: false
    autoDeploy: true
